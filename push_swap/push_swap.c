#include "push_swap.h"


void	error(void)
{
	ft_printf("Error");
}

int	check_arg(char **av)
{
	while (av)
	{
		if ()
	}
}

int	main(int ac, char **av)
{
	if (ac != 1 && check_arg(av))
		return (error(), 0);
	return (0);
}